// Insertion Sort
InsertionSort(A)
  for j in 2 to Length(A)
    key = A[j]
    //Inserting A[j] in sorted sequence of A[1 to j-1]
    i = j-1
    while i>0 and A[j] > key
      A[i+1] = A[i]
      i = i-1
    A[i+1] = key
